import React from 'react';
import { motion } from 'framer-motion';
import { 
  FlaskConical, 
  Activity, 
  Pill, 
  Microscope, 
  Users, 
  Database,
  ArrowUpRight,
  ArrowRight
} from 'lucide-react';
import { Button } from "@/components/ui/button";

const services = [
  {
    icon: FlaskConical,
    title: "Phase I Clinical Studies",
    description: "First-in-human safety and tolerability assessments with comprehensive pharmacokinetic profiling in healthy volunteers.",
    features: ["Single & Multiple Ascending Dose", "Safety Monitoring", "PK Sampling"],
  },
  {
    icon: Activity,
    title: "Bioequivalence Studies",
    description: "Comparative bioavailability studies demonstrating therapeutic equivalence for generic drug development and registration.",
    features: ["Crossover Designs", "Fed/Fasted Studies", "Regulatory Submission"],
  },
  {
    icon: Pill,
    title: "Bioavailability Studies",
    description: "Absolute and relative bioavailability assessments to characterize drug absorption and systemic exposure.",
    features: ["Formulation Comparison", "Route of Administration", "Food Effect Studies"],
  },
  {
    icon: Microscope,
    title: "PK/PD Studies",
    description: "Integrated pharmacokinetic and pharmacodynamic evaluations to establish dose-response relationships.",
    features: ["Exposure-Response Analysis", "Biomarker Assessment", "Modeling Support"],
  },
  {
    icon: Users,
    title: "First-In-Human Studies",
    description: "Pioneering early clinical development with rigorous safety protocols and adaptive trial designs.",
    features: ["Sentinel Dosing", "Real-time Safety Review", "Dose Escalation"],
  },
  {
    icon: Database,
    title: "Data Management & Statistics",
    description: "Fully validated electronic data capture systems with in-house servers ensuring high security and quality data monitoring. Statistical analyses performed using SAS 9.4+ and Phoenix WinNonlin 8.3+.",
    features: ["FDA-Approved EDC with Full Audit Trail", "GXP-Compliant Validation", "Expert Statistical Analysis"],
  },
];

export default function ServicesSection() {
  return (
    <section className="py-24 lg:py-32 bg-white" id="services">
      <div className="max-w-7xl mx-auto px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6 }}
          className="text-center mb-20"
        >
          <span className="text-blue-600 font-medium tracking-wide uppercase text-sm">Our Expertise</span>
          <h2 className="mt-4 text-4xl lg:text-5xl font-light text-slate-900">
            Comprehensive <span className="font-semibold">Clinical Services</span>
          </h2>
          <p className="mt-6 text-lg text-slate-600 max-w-2xl mx-auto">
            From first-in-human studies to bioequivalence trials, we deliver 
            precision clinical research with European regulatory excellence.
          </p>
        </motion.div>

        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          {services.map((service, index) => (
            <motion.div
              key={service.title}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: index * 0.1 }}
              className="group relative bg-slate-50 hover:bg-blue-900 rounded-2xl p-8 transition-all duration-500 cursor-pointer overflow-hidden"
            >
              <div className="relative z-10">
                <div className="w-16 h-16 rounded-2xl bg-blue-900 group-hover:bg-white/20 flex items-center justify-center mb-6 transition-colors duration-500 shadow-lg shadow-blue-900/20 group-hover:shadow-white/10">
                  <service.icon className="h-8 w-8 text-white transition-colors duration-500 stroke-[2.5]" />
                </div>

                <h3 className="text-xl font-semibold text-slate-900 group-hover:text-white mb-3 transition-colors duration-500">
                  {service.title}
                </h3>

                <p className="text-slate-600 group-hover:text-blue-100 mb-6 transition-colors duration-500 leading-relaxed">
                  {service.description}
                </p>

                <div className="space-y-2">
                  {service.features.map((feature) => (
                    <div 
                      key={feature} 
                      className="flex items-center gap-2 text-sm text-slate-500 group-hover:text-blue-200 transition-colors duration-500"
                    >
                      <div className="w-1.5 h-1.5 rounded-full bg-blue-400 group-hover:bg-blue-200" />
                      {feature}
                    </div>
                  ))}
                </div>

                <div className="mt-8 flex items-center gap-2 text-blue-600 group-hover:text-white font-medium transition-colors duration-500">
                  Learn More
                  <ArrowUpRight className="h-4 w-4 transition-transform group-hover:translate-x-1 group-hover:-translate-y-1" />
                </div>
              </div>

              {/* Decorative element */}
              <div className="absolute -bottom-20 -right-20 w-40 h-40 rounded-full bg-blue-500/10 group-hover:bg-white/10 transition-colors duration-500" />
            </motion.div>
          ))}
        </div>
        </div>

        {/* Global Sponsors CTA */}
        <motion.div
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ duration: 0.6 }}
        className="mt-20 text-center"
        >
        <div className="bg-gradient-to-br from-blue-900 to-blue-800 rounded-3xl p-12">
          <h3 className="text-3xl font-semibold text-white mb-4">
            Global Sponsors
          </h3>
          <p className="text-blue-200 text-lg mb-8 max-w-2xl mx-auto">
            Looking for Phase II-IV clinical research services? We partner with leading organizations worldwide.
          </p>
          <Button 
            size="lg"
            className="bg-white hover:bg-slate-100 text-blue-900 px-8 py-6 text-base font-medium rounded-xl"
            onClick={() => {
              const element = document.querySelector('#contact');
              if (element) element.scrollIntoView({ behavior: 'smooth' });
            }}
          >
            Phase II-IV Services
            <ArrowRight className="ml-2 h-5 w-5" />
          </Button>
        </div>
        </motion.div>
        </section>
        );
        }