import React from 'react';
import { motion } from 'framer-motion';
import { FileCheck, Rocket, Users, Zap, CheckCircle2 } from 'lucide-react';

const services = [
  {
    icon: FileCheck,
    title: "Protocol Development",
    description: "Expert support for comprehensive protocol development aligned with EU regulations.",
  },
  {
    icon: CheckCircle2,
    title: "Ethics & Authority Submissions",
    description: "Complete management of ethics committee and regulatory authority submissions.",
  },
  {
    icon: Zap,
    title: "Fast-Track CTIS Submissions",
    description: "Leverage our fast-pace CTIS submission options for accelerated approval timelines.",
  },
  {
    icon: Users,
    title: "Rapid Recruitment Strategy",
    description: "Efficient study start-up with proven recruitment strategies and established volunteer database.",
  },
];

export default function RegulatoryServicesSection() {
  return (
    <section className="py-24 lg:py-32 bg-gradient-to-br from-slate-900 via-blue-950 to-slate-900 relative overflow-hidden">
      {/* Background pattern */}
      <div className="absolute inset-0 bg-[linear-gradient(rgba(59,130,246,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(59,130,246,0.03)_1px,transparent_1px)] bg-[size:60px_60px]" />
      
      {/* Decorative elements */}
      <div className="absolute top-0 right-0 w-96 h-96 bg-blue-600/10 rounded-full blur-3xl" />
      <div className="absolute bottom-0 left-0 w-96 h-96 bg-blue-800/10 rounded-full blur-3xl" />

      <div className="max-w-7xl mx-auto px-6 lg:px-8 relative z-10">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6 }}
          className="text-center mb-16"
        >
          <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-blue-900/30 border border-blue-800/40 mb-6">
            <Rocket className="h-4 w-4 text-blue-400" />
            <span className="text-blue-200 text-sm font-medium tracking-wide">Fast-Track Your Study</span>
          </div>
          
          <h2 className="text-4xl lg:text-5xl font-light text-white leading-tight">
            Regulatory Services &
            <span className="block font-semibold bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent">
              Study Startup
            </span>
          </h2>
          
          <p className="mt-6 text-lg text-slate-300 max-w-3xl mx-auto leading-relaxed">
            We provide comprehensive support for protocol development, ethics and authority submissions, 
            and regulatory interactions in the EU. We ensure fast and efficient study start-up with 
            rapid recruitment strategy and our fast-pace CTIS submission options.
          </p>
        </motion.div>

        <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
          {services.map((service, index) => (
            <motion.div
              key={service.title}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: index * 0.1 }}
              className="bg-slate-800/50 backdrop-blur-sm border border-slate-700/50 rounded-2xl p-6 hover:bg-slate-800/70 transition-all duration-300 hover:shadow-xl hover:shadow-blue-900/20"
            >
              <div className="w-14 h-14 rounded-xl bg-blue-900/30 border border-blue-800/50 flex items-center justify-center mb-6">
                <service.icon className="h-7 w-7 text-blue-400" />
              </div>
              <h3 className="text-lg font-semibold text-white mb-3">
                {service.title}
              </h3>
              <p className="text-slate-400 leading-relaxed">
                {service.description}
              </p>
            </motion.div>
          ))}
        </div>

        {/* Call to action */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6, delay: 0.4 }}
          className="mt-12 text-center"
        >
          <p className="text-blue-200 text-lg">
            Ready to fast-track your clinical study approval?
          </p>
        </motion.div>
      </div>
    </section>
  );
}