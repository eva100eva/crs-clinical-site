import React from 'react';
import { motion } from 'framer-motion';
import { Database, CheckCircle2, ArrowRight, Server, BarChart3, Shield } from 'lucide-react';
import { Button } from "@/components/ui/button";
import Footer from '../components/crs/Footer';

export default function DataManagementStatistics() {
  const handleContactClick = () => {
    window.location.href = '/#contact';
  };

  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section */}
      <section className="relative pt-32 pb-20 bg-gradient-to-br from-slate-900 via-blue-900 to-slate-800 overflow-hidden">
        <div className="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1551288049-bebda4e38f71?q=80&w=2070')] bg-cover bg-center opacity-10" />
        <div className="max-w-7xl mx-auto px-6 lg:px-8 relative z-10">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            className="max-w-3xl"
          >
            <div className="flex items-center gap-3 mb-6">
              <div className="w-14 h-14 rounded-xl bg-blue-600 flex items-center justify-center">
                <Database className="h-7 w-7 text-white" />
              </div>
              <span className="text-blue-300 text-sm font-medium tracking-wide uppercase">Clinical Services</span>
            </div>
            <h1 className="text-5xl lg:text-6xl font-light text-white mb-6 leading-tight">
              Data Management <span className="font-semibold">& Statistics</span>
            </h1>
            <p className="text-xl text-slate-300 leading-relaxed">
              Fully validated electronic data capture systems with in-house servers ensuring high security and quality data monitoring.
            </p>
          </motion.div>
        </div>
      </section>

      {/* Overview */}
      <section className="py-20 bg-white">
        <div className="max-w-7xl mx-auto px-6 lg:px-8">
          <div className="grid lg:grid-cols-2 gap-16 items-center">
            <motion.div
              initial={{ opacity: 0, x: -20 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.6 }}
            >
              <h2 className="text-3xl lg:text-4xl font-semibold text-slate-900 mb-6">
                Quality Data from Start to Finish
              </h2>
              <p className="text-lg text-slate-600 leading-relaxed mb-6">
                Our data management and statistical services ensure the highest quality data collection, processing, and analysis throughout your clinical study lifecycle.
              </p>
              <p className="text-lg text-slate-600 leading-relaxed">
                With fully validated EDC systems, in-house servers, and expert statisticians, we deliver reliable, audit-ready data that meets global regulatory standards.
              </p>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, x: 20 }}
              whileInView={{ opacity: 1, x: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.6 }}
              className="grid grid-cols-2 gap-6"
            >
              <div className="bg-slate-50 rounded-2xl p-6">
                <Server className="h-8 w-8 text-blue-600 mb-4" />
                <h3 className="font-semibold text-slate-900 mb-2">In-House Servers</h3>
                <p className="text-sm text-slate-600">High security & data protection</p>
              </div>
              <div className="bg-slate-50 rounded-2xl p-6">
                <Shield className="h-8 w-8 text-blue-600 mb-4" />
                <h3 className="font-semibold text-slate-900 mb-2">FDA Approved</h3>
                <p className="text-sm text-slate-600">Full audit trail & GXP compliance</p>
              </div>
              <div className="bg-slate-50 rounded-2xl p-6">
                <BarChart3 className="h-8 w-8 text-blue-600 mb-4" />
                <h3 className="font-semibold text-slate-900 mb-2">SAS Analysis</h3>
                <p className="text-sm text-slate-600">Version 9.4 or later</p>
              </div>
              <div className="bg-slate-50 rounded-2xl p-6">
                <Database className="h-8 w-8 text-blue-600 mb-4" />
                <h3 className="font-semibold text-slate-900 mb-2">Phoenix WinNonlin</h3>
                <p className="text-sm text-slate-600">PK parameter calculation</p>
              </div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Electronic Data Capture */}
      <section className="py-20 bg-slate-50">
        <div className="max-w-7xl mx-auto px-6 lg:px-8">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
            className="text-center mb-16"
          >
            <h2 className="text-3xl lg:text-4xl font-semibold text-slate-900 mb-4">
              Electronic Data Capture System
            </h2>
            <p className="text-lg text-slate-600 max-w-2xl mx-auto">
              GXP-compliant EDC with full validation and audit trail
            </p>
          </motion.div>

          <div className="grid md:grid-cols-2 gap-8">
            {[
              {
                title: "Fully Validated EDC Systems",
                description: "Our electronic data capture systems are fully validated according to Good Practices for computerised systems in regulated GXP environments (Pharmaceutical Inspection Convention).",
                features: ["FDA 21 CFR Part 11 compliant", "Full system validation", "Regular re-validation", "Comprehensive documentation"]
              },
              {
                title: "In-House Server Infrastructure",
                description: "In-house servers ensure the highest level of security and quality in data monitoring, with complete control over data access and storage.",
                features: ["High-security servers", "Encrypted data storage", "Redundant backups", "24/7 monitoring"]
              },
              {
                title: "e-CRF Design & Maintenance",
                description: "Custom e-CRF design tailored to your protocol with built-in edit checks and data validation rules for real-time quality assurance.",
                features: ["Custom form design", "Edit checks & validations", "User-friendly interface", "Ongoing maintenance"]
              },
              {
                title: "Full Audit Trail",
                description: "Complete audit trail maintained for all data entry, modifications, and queries ensuring full transparency and regulatory compliance.",
                features: ["Complete change history", "User identification", "Timestamp recording", "Reason for change tracking"]
              }
            ].map((feature, index) => (
              <motion.div
                key={feature.title}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                className="bg-white rounded-2xl p-8 shadow-sm hover:shadow-lg transition-shadow"
              >
                <h3 className="text-xl font-semibold text-slate-900 mb-3">{feature.title}</h3>
                <p className="text-slate-600 mb-6 leading-relaxed">{feature.description}</p>
                <div className="space-y-2">
                  {feature.features.map((item) => (
                    <div key={item} className="flex items-center gap-2">
                      <CheckCircle2 className="h-4 w-4 text-blue-600 flex-shrink-0" />
                      <span className="text-sm text-slate-700">{item}</span>
                    </div>
                  ))}
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Statistical Analysis */}
      <section className="py-20 bg-white">
        <div className="max-w-7xl mx-auto px-6 lg:px-8">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
            className="text-center mb-16"
          >
            <h2 className="text-3xl lg:text-4xl font-semibold text-slate-900 mb-4">
              Expert Statistical Analysis
            </h2>
          </motion.div>

          <div className="grid md:grid-cols-3 gap-8 mb-12">
            {[
              {
                title: "SAS Statistical Software",
                description: "All descriptive summaries and statistical analyses performed using SAS Version 9.4 or later (SAS Institute, Inc., Cary, NC) or equivalent programs.",
                icon: BarChart3
              },
              {
                title: "Phoenix WinNonlin",
                description: "PK parameters calculated using Phoenix WinNonlin® version 8.3 or later (Certara, U.S.A.) with validated methodologies.",
                icon: Database
              },
              {
                title: "Independent Experts",
                description: "Our statisticians are highly appraised independent experts who cooperate with Faculty of Pharmacy and Faculty of Medicine in Ljubljana, Slovenia.",
                icon: Shield
              }
            ].map((item, index) => (
              <motion.div
                key={item.title}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                className="bg-slate-50 rounded-2xl p-8"
              >
                <item.icon className="h-10 w-10 text-blue-600 mb-4" />
                <h3 className="text-lg font-semibold text-slate-900 mb-3">{item.title}</h3>
                <p className="text-slate-600 leading-relaxed">{item.description}</p>
              </motion.div>
            ))}
          </div>

          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
            className="bg-gradient-to-br from-blue-900 to-blue-800 rounded-3xl p-12"
          >
            <h3 className="text-2xl font-semibold text-white mb-6">Statistical Services Include</h3>
            <div className="grid md:grid-cols-2 gap-6 text-blue-100">
              {[
                "Statistical Analysis Plans (SAP)",
                "Sample size and power calculations",
                "Randomization and blinding",
                "Descriptive statistics",
                "Inferential statistics (t-tests, ANOVA, etc.)",
                "Bioequivalence analysis (90% CI)",
                "PK parameter calculations",
                "Population PK modeling support",
                "Interim analyses",
                "Final statistical reports",
                "Tables, listings, and figures (TLFs)",
                "Regulatory submission support"
              ].map((service, index) => (
                <div key={index} className="flex items-center gap-3">
                  <CheckCircle2 className="h-5 w-5 text-blue-300 flex-shrink-0" />
                  <span>{service}</span>
                </div>
              ))}
            </div>
          </motion.div>
        </div>
      </section>

      {/* Data Quality */}
      <section className="py-20 bg-slate-50">
        <div className="max-w-7xl mx-auto px-6 lg:px-8">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
            className="text-center mb-16"
          >
            <h2 className="text-3xl lg:text-4xl font-semibold text-slate-900 mb-4">
              Quality Assurance Throughout
            </h2>
          </motion.div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {[
              {
                title: "Data Quality Monitoring",
                description: "Continuous monitoring of data quality with automated checks and manual review to ensure accuracy and completeness."
              },
              {
                title: "Query Management",
                description: "Efficient query management system for resolving data discrepancies with full documentation and tracking."
              },
              {
                title: "Database Lock Procedures",
                description: "Rigorous database lock procedures ensuring data integrity before statistical analysis and reporting."
              },
              {
                title: "Data Cleaning",
                description: "Comprehensive data cleaning processes with validation checks, outlier review, and consistency verification."
              },
              {
                title: "SDTM & ADaM Datasets",
                description: "CDISC-compliant datasets for regulatory submissions including SDTM and ADaM formats."
              },
              {
                title: "Regulatory Readiness",
                description: "All data management and statistical outputs prepared for regulatory inspection and submission."
              }
            ].map((item, index) => (
              <motion.div
                key={item.title}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                className="bg-white rounded-2xl p-6"
              >
                <h3 className="text-lg font-semibold text-slate-900 mb-3">{item.title}</h3>
                <p className="text-slate-600 leading-relaxed">{item.description}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="py-20 bg-gradient-to-br from-blue-900 to-blue-800">
        <div className="max-w-4xl mx-auto px-6 lg:px-8 text-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6 }}
          >
            <h2 className="text-3xl lg:text-4xl font-semibold text-white mb-6">
              Ensure Data Quality & Integrity
            </h2>
            <p className="text-xl text-blue-100 mb-8">
              Partner with data management and statistics experts you can trust
            </p>
            <Button 
              onClick={handleContactClick}
              className="bg-white text-blue-900 hover:bg-blue-50 px-8 py-6 text-lg rounded-xl"
            >
              Contact Us Today
              <ArrowRight className="ml-2 h-5 w-5" />
            </Button>
          </motion.div>
        </div>
      </section>

      <Footer />
    </div>
  );
}